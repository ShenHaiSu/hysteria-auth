{
  "name": "back-end",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run migrate && bun run src/index.ts",
    "start": "bun run migrate && bun run src/index.ts",
    "migrate": "bun run src/db/migrator.ts"
  }
}
